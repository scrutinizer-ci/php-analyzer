<?php

if ($foo) {
    if ($bar) echo 'foo';
}

-- CONFIG --
return array('loops_must_use_braces' => true);

-- COMMENTS --
Line 4: Please always use braces to surround the code block of IF statements.
